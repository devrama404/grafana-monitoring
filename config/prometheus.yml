global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter_vm1'
    static_configs:
      - targets: ['172.31.2.212:9100']
        labels:
          hostname: shop-devrama
          site: cloud-aws
          env: production
